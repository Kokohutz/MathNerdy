# ============== Configuration ==============
# Minimal configuration to get started.
# Get your API keys from the following URLs:
# - OpenAI: https://platform.openai.com/api-keys
# - Groq (STT only): https://console.groq.com/keys/
# - ElevenLabs: https://elevenlabs.io/app/speech-synthesis/text-to-speech
# =============================================

# === LLM Configuration (OpenAI) ===
# The reasoning agent uses the OpenAI SDK; point it at the OpenAI API.
# gpt-5.4 is the cost/quality sweet spot and supports Structured Outputs
# (response_format json_schema). Cheaper: gpt-5.4-mini / gpt-5.4-nano.
# Higher quality: gpt-5.5. The model must support Structured Outputs.
LLM_API_KEY="your_openai_api_key_here"
LLM_BASE_URL="https://api.openai.com/v1"
LLM_MODEL_ID="gpt-5.4"

# === Speech-to-Text (STT) Configuration ===
# STT is a separate service (not the chat LLM). Groq Whisper is the default;
# alternatives are "deepgram" and "faster_whisper" (local, no key).
STT_PROVIDER="groq"
GROQ_STT_API_KEY="your_groq_api_key_here"

# === Text-to-Speech (TTS) Configuration ===
TTS_PROVIDER="elevenlabs"       # Alternative providers: "deepgram", "openai", "cartesia"
ELEVENLABS_API_KEY="your_elevenlabs_api_key"
ELEVENLABS_VOICE_ID="Xb7hH8MSUJpSbSDYk0k2"

# =============================================
# Customize your configuration below
# =============================================

REASONING_DIRECTORY="./reasoning/"
REDIS_HOST="xrx-redis"
LLM_OBSERVABILITY_LIBRARY="none"

# === Frontend ===
# Selects the UI skin in nextjs-client/src/app/types/skinConfig.ts
NEXT_PUBLIC_AGENT="math-tutor"

# === Handwriting whiteboard (vision) ===
# The /api/handwriting route reads the student's handwritten work with a
# vision model via the OpenAI API. Used server-side only. VISION_MODEL_ID
# must be a vision-capable model. If OPENAI_API_KEY is unset the route falls
# back to LLM_API_KEY, so a single OpenAI key works for everything.
# gpt-5.4 is vision-capable and strong at handwriting; gpt-5.5 is higher quality.
OPENAI_API_KEY="your_openai_api_key_here"
VISION_MODEL_ID="gpt-5.4"